# JARVIS Vision / Touchpad engine dependencies.
# Auto-installed by vision_launcher.py into a project-local virtual environment
# (NOT into the user's global Python).  Versions are pinned to the latest
# release line known to work together on Python 3.11 / 3.12 on Windows.

mediapipe==0.10.21
opencv-python==4.10.0.84
numpy>=1.26,<2.2
requests>=2.31
pyautogui>=0.9.54
# pyautogui's Windows mouse-button backend (used implicitly).
# Only pulled in on Windows; pip on other OSes will skip it silently.
pywin32>=306; sys_platform == "win32"
